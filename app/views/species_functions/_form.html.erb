<%= form_with(model: species_function) do |form| %>
  <% if species_function.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(species_function.errors.count, "error") %> prohibited this species function from being saved:</h2>

      <ul>
        <% species_function.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
